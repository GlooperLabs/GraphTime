sudo: false
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
cache:
  apt: true
  directories:
  - $HOME/.cache/pip
addons:
  apt:
    packages:
      - python-scipy
dist: trusty
install:
  - pip install nose
script: nosetests
